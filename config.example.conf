# 周报生成器配置文件
# 使用前请复制为 config.conf：cp config.example.conf config.conf

# 项目目录列表，每行一个绝对路径
PROJECT_DIRS=(
    "/path/to/your/project-a"
    "/path/to/your/project-b"
)

# 可选：指定 git author（留空则自动从各项目的 git config user.name 读取）
AUTHOR=""
